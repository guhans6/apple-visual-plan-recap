export const COLLAB_PLUGIN_DISABLED = true;

/**
 * Keep the `collab` plugin stem present while intentionally disabling
 * real-time multi-user editing in the local-only companion surface.
 */
export default function collabPluginDisabled() {}
